#include "main.h"

/**
 *_strlen - count the length of a string
 *@s: counting string
 *Return: lenght of string
 */
int _strlen(char *s)
{
	int lenght;

	for (lenght = 0; s[lenght] != '\0'; lenght++)
	{
	}
	return (lenght);
}
